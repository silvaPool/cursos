import { Box } from "@mui/material";
import styled from "styled-components";

const StyledReis = styled(Box) `
    & {
        background-color: green;
        flex-grow: 1;
        color: black;
        
    }
`

export default StyledReis;